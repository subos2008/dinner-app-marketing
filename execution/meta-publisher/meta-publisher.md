This is a new SPA, again mobile optimized but with a specific focus. Here the focus is on choosing ads that have been generated in the creative SPA and pushing them up to ad sets in meta. We want to generally keep things fairly simple. We will want to perhaps support multiple campaigns. and we will want to create or import ad sets from Meta. We have installed something that is an MPC, MCP, but I guess we will use Meta's API directly, presumably using an SDK. So the way I'm imagining this is that the ads generated from the Creative SPA are available as a list. And we have various metadata about those ads. For example, their status from a publishing perspective. Are they accepted or selected for publishing? I was thinking very much to have a Terraform style thing here where we have Terraform plan and then kind of Terraform apply equivalents. So each ad set would be aware of what we think its current state is, and then we can set what the user sets to the desired state so like active and that will set whether the ad gets sent up to the ad set once it's been uploaded to Meta we're syncing whatever ID that ad has in Meta into our local database And then... Well I suppose that's the main functionality really, is getting things out of our local tool here into ads in ad sets. And we could create the ad sets through this tool or through Meta's desktop interface. It might be better to do that through Meta, I guess. So I want to brainstorm and plan this out. I want to keep it simple. I don't want a lot of code to maintain unless it's necessary. But I would like to be able to generate the creative images in these applications and then upload them to Meta.
