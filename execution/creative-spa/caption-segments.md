If I have a segment selected and generate a caption, it doesn't tag the caption with the segment. 

When adding a new caption manually (on the captions list page) we don't update the loaded list of captions. Need to refresh the page.

