We will be publishing things to Facebook and once we've published an advert or used an image in an advert or used a caption in an advert, I think that we want to set a tag, a flag in the database on those items which makes them locked immutable. And I think we keep things fairly simple in the creative editing app. Things just can't be edited if they're locked immutable, i.e. they're in use by something. And maybe we display a little locked icon. This prevents us deleting things that are in use. and I think that we want the database rules to enforce this. I.e. you can't delete something if it's got that flag set in the table or edit it.
